# 测试命令
```aiignore
MODEL_ID=$(curl -s http://127.0.0.1:30015/v1/models |
    python3 -c 'import sys,json; print(json.load(sys.stdin)["data"][0]["id"])') && \
  curl -sS http://127.0.0.1:30015/v1/chat/completions \
    -H "Content-Type: application/json; charset=utf-8" \
    -d "{
      \"model\": \"$MODEL_ID\",
      \"messages\": [
        {
          \"role\": \"user\",
          \"content\": \"请回复：中文 Unicode 转义测试成功。\"
        }
      ],
      \"temperature\": 0,
      \"max_tokens\": 64
    }" |
  python3 -c 'import sys,json; data=json.load(sys.stdin); print(json.dumps(data, ensure_ascii=False, indent=2))'

```
