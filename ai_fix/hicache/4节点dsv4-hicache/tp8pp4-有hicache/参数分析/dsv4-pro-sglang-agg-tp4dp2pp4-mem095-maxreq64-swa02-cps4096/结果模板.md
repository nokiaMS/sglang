# dsv4-pro-sglang-agg-tp4dp2pp4-mem095-maxreq64-swa02-cps4096

## Env

* DeepSeek-V4-Pro
* 4 nodes, 8x NVIDIA H800 80GB HBM3 per node (32 GPUs total)
* sglang v0.5.12.post1

## Script

### Serve
SGLANG_SHARED_EXPERT_TP1=1 \
SGLANG_ENABLE_UNIFIED_RADIX_TREE=1 \
python3 -m sglang.launch_server \
--model-path /userdata/dsv4/DeepSeek-V4-Pro \
--served-model-name deepseek-v4-pro \
--trust-remote-code \
--tp 4 \
--dp-size 2 \
--enable-dp-attention \
--pp-size 4 \
--moe-runner-backend marlin \
--nnodes 4 \
--node-rank 0 \
--dist-init-addr 172.16.107.21:20000 \
--context-length 1048576 \
--mem-fraction-static 0.95 \
--disable-cuda-graph \
--disable-overlap-schedule \
--watchdog-timeout 3600 \
--enable-hierarchical-cache \
--hicache-ratio 2.0 \
--hicache-write-policy write_through \
--hicache-io-backend direct \
--max-running-requests 64 \
--swa-full-tokens-ratio 0.2 \
--chunked-prefill-size 4096 \
--host 0.0.0.0 \
--port 8080

### Bench
python3 -m sglang.bench_serving \
--backend sglang \
--base-url http://127.0.0.1:8080 \
--model /userdata/dsv4/DeepSeek-V4-Pro \
--served-model-name deepseek-v4-pro \
--dataset-name random-ids \
--random-input-len 50000 \
--random-output-len 200 \
--num-prompts 100 \
--max-concurrency 10 \
--seed 123

## C10

### R1
```
FAILED: C10 warmup failed because the sglang service crashed before the benchmark request completed.

Bench log:
- c10_logs/bench_c10_r1.log
- ConnectionRefusedError: Cannot connect to host 127.0.0.1:8080

Server logs:
- c10_logs/node0.log
- c10_logs/node1.log
- c10_logs/node2.log
- c10_logs/node3.log

Key server errors:
- RuntimeError: gloo transport tcp pair Connection closed by peer
- Scheduler or DataParallelController terminated with -3
- Application startup failed. Exiting.
```

### R2
```
NOT RUN: C10 R1 crashed the service during warmup.
```

### R3
```
NOT RUN: C10 R1 crashed the service during warmup.
```

## C30

### R1
```
NOT RUN: service crashed during C10 warmup.
```

### R2
```
NOT RUN: service crashed during C10 warmup.
```

### R3
```
NOT RUN: service crashed during C10 warmup.
```

## C60

### R1
```
NOT RUN: service crashed during C10 warmup.
```

### R2
```
NOT RUN: service crashed during C10 warmup.
```

### R3
```
NOT RUN: service crashed during C10 warmup.
```
