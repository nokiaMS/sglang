# dsv4-pro-sglang-agg-tp4dp2pp4-mem095-maxreq64-swa02-cps4096-nohicache

## Env

* DeepSeek-V4-Pro
* 4 nodes, 8x NVIDIA H800 80GB HBM3 per node (32 GPUs total)
* sglang v0.5.12.post1
* HiCache disabled

## Script

### Serve
SGLANG_SHARED_EXPERT_TP1=1 \
SGLANG_ENABLE_UNIFIED_RADIX_TREE=1 \
python3 -m sglang.launch_server \
--model-path /userdata/dsv4/DeepSeek-V4-Pro \
--served-model-name deepseek-v4-pro \
--trust-remote-code \
--tp 4 \
--dp-size 2 \
--enable-dp-attention \
--pp-size 4 \
--moe-runner-backend marlin \
--nnodes 4 \
--node-rank 0 \
--dist-init-addr 172.16.107.21:20000 \
--context-length 1048576 \
--mem-fraction-static 0.95 \
--disable-cuda-graph \
--disable-overlap-schedule \
--watchdog-timeout 3600 \
--max-running-requests 64 \
--swa-full-tokens-ratio 0.2 \
--chunked-prefill-size 4096 \
--host 0.0.0.0 \
--port 8080

### Bench
```
NOT RUN: service crashed during startup/warmup before benchmark execution.
```

## Startup

### Result
```
FAILED: no-HiCache service still crashed before benchmark execution.

Key evidence:
- startup_logs/node0.log shows enable_hierarchical_cache=False.
- startup_logs/node1.log shows AttributeError: _DpGatheredBufferWrapper has no attribute _dp_max_padding.
- startup_logs/node0.log, node2.log, node3.log show gloo Connection closed by peer as cascading PP communication failures.
```

## C10

### R1
```
NOT RUN: service crashed before C10 benchmark.
```

### R2
```
NOT RUN: service crashed before C10 benchmark.
```

### R3
```
NOT RUN: service crashed before C10 benchmark.
```

## C30

### R1
```
NOT RUN: service crashed before C30 benchmark.
```

### R2
```
NOT RUN: service crashed before C30 benchmark.
```

### R3
```
NOT RUN: service crashed before C30 benchmark.
```

## C60

### R1
```
NOT RUN: service crashed before C60 benchmark.
```

### R2
```
NOT RUN: service crashed before C60 benchmark.
```

### R3
```
NOT RUN: service crashed before C60 benchmark.
```
