- 按照如下步骤执行测试
  - 服务器连接信息位于 E:\code\ai_fix\hicache\服务器连接信息.md 中。
  - pod操作命令位于 E:\code\ai_fix\hicache\pod操作命令.md 中。
  - 待测试文件夹为 dsv4-pro-sglang-agg-tp8pp4-mem090-maxreq64-swa01
  - 测试执行
    - 检查pod中是否已经有sglang服务正在运行，如果有，则关闭。
    - 如果pod重启了，那么首先需要按照 E:\code\ai_fix\hicache\4节点dsv4-hicache\tp8pp4-有hicache\代码修改 中的说明修改源代码。
    - 执行待测试文件夹中的测试，待测试文件夹作为“当前文件夹”。
      - 按照“当前文件夹”中文档 启动命令.md 进行服务部署与测试。
        - 测试过程中，服务程序如果发生崩溃或者退出，那么需要把4个服务节点的日志拷贝到"当前文件夹“的c10_logs文件夹下。
      - C10测试执行完成之后：
        - 把第一次测试结果中 ============ Serving Benchmark Result ============ 与 ============ Serving Benchmark Result ============ 中间的内容替换到 结果模板.md 的 <C10_R1测试结果> 位置，如果没有找到，则替换<C10_R1测试结果>为pass。
        - 把第二次测试结果中 ============ Serving Benchmark Result ============ 与 ============ Serving Benchmark Result ============ 中间的内容替换到 结果模板.md 的 <C10_R2测试结果> 位置，如果没有找到，则替换<C10_R1测试结果>为pass。
        - 把第三次测试结果中 ============ Serving Benchmark Result ============ 与 ============ Serving Benchmark Result ============ 中间的内容替换到 结果模板.md 的 <C10_R3测试结果> 位置，如果没有找到，则替换<C10_R1测试结果>为pass。
        - C10测试完成之后，拷贝服务端4节点的启动日志与测试程序日志到“当前文件夹”的c10_logs文件夹下。
      - 然后执行C30测试。
        - 测试过程中，服务程序如果发生崩溃或者退出，那么需要把4个服务节点的日志拷贝到"当前文件夹“的c10_logs文件夹下。
      - C30测试执行完成之后：
        - 把第一次测试结果中 ============ Serving Benchmark Result ============ 与 ============ Serving Benchmark Result ============ 中间的内容替换到 结果模板.md 的 <C30_R1测试结果> 位置，如果没有找到，则替换<C10_R1测试结果>为pass。
        - 把第二次测试结果中 ============ Serving Benchmark Result ============ 与 ============ Serving Benchmark Result ============ 中间的内容替换到 结果模板.md 的 <C30_R2测试结果> 位置，如果没有找到，则替换<C10_R1测试结果>为pass。
        - 把第三次测试结果中 ============ Serving Benchmark Result ============ 与 ============ Serving Benchmark Result ============ 中间的内容替换到 结果模板.md 的 <C30_R3测试结果> 位置，如果没有找到，则替换<C10_R1测试结果>为pass。
        - C30测试完成之后，拷贝服务端4节点的启动日志与测试程序日志到“当前文件夹”的c30_logs文件夹下。
      - 然后执行C60测试。
        - 测试过程中，服务程序如果发生崩溃或者退出，那么需要把4个服务节点的日志拷贝到"当前文件夹“的c10_logs文件夹下。
      - C60测试执行完成之后：
        - 把第一次测试结果中 ============ Serving Benchmark Result ============ 与 ============ Serving Benchmark Result ============ 中间的内容替换到 结果模板.md 的 <C60_R1测试结果> 位置，如果没有找到，则替换<C10_R1测试结果>为pass。
        - 把第二次测试结果中 ============ Serving Benchmark Result ============ 与 ============ Serving Benchmark Result ============ 中间的内容替换到 结果模板.md 的 <C60_R2测试结果> 位置，如果没有找到，则替换<C10_R1测试结果>为pass。
        - 把第三次测试结果中 ============ Serving Benchmark Result ============ 与 ============ Serving Benchmark Result ============ 中间的内容替换到 结果模板.md 的 <C60_R3测试结果> 位置，如果没有找到，则替换<C10_R1测试结果>为pass。
        - C60测试完成之后，拷贝服务端4节点的启动日志与测试程序日志到“当前文件夹”的c60_logs文件夹下。
      - 对测试结果进行分析，分析结果形成新的文件存储到“当前文件夹”。
      - 把启动命令.md中的node 0的启动命令替换到文件结果模板.md中的 <node 0的启动脚本> 位置。
      - 把启动命令.md中的c10的压测命令替换到文件结果模板.md中的 <c10测试命令> 位置。
    - 测试结束后 提示 “测试结束！”
